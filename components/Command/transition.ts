import { Transition, Variants } from "framer-motion";

export const overlayTransition: Transition = {
    type: "tween",
    duration: 0.1,
};

export const commandTransition: Transition = {
    type: "tween",
    duration: 0.3,
};
