import {
    useNavigationMenu,
    useThemeActions,
} from "@components/Command/commands";

const RegisterMenu = () => {
    useThemeActions();
    useNavigationMenu();

    return <></>;
};

export default RegisterMenu;
