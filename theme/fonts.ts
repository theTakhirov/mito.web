import { RecursiveObject } from "@chakra-ui/react";

const fonts: RecursiveObject<string> = {
    body: "TT Firs",
    heading: "TT Firs",
    handWriting: "Scratchy",
    mono: "InGram",
};

export default fonts;
